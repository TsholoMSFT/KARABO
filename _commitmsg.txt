feat(blueprint): Blueprint Copilot rail (Option A+B) — AI suggestions + auto-IQ + cost meter

What this adds:
- src/lib/blueprint-copilot-service.ts
  * nextSuggestions(state): deterministic next-best-action engine
  * runIQDiscovery(customer): parallel pull from Graph/Foundry/Fabric/Work + grounding note
  * generateStarterUseCases(): 3 candidate use cases grounded on estate + IQ (analysis → gpt-4o-mini)
  * recommendArchetype(): LLM tiebreaker over recommender (analysis → gpt-4o-mini)
  * generateExecBrief(): board-ready 1-pager (executive → gpt-4o)
  * Per-customer USD spend ledger in localStorage with budget cap (default $0.50)

- src/components/solution-blueprint/BlueprintCopilotRail.tsx
  * Sticky right-rail card; mode select (Off | Suggest | Autopilot, default Suggest)
  * Shows top 3 suggestions with 1-click run, est cost in cents per action
  * Live $ spend bar + per-model breakdown
  * Autopilot auto-fires safe-to-autorun actions (currently: pull-iq)

- src/components/SolutionBlueprintWorkspace.tsx
  * 2-col layout when AI mode is on (1fr | 340px rail)
  * Controlled tabs so the rail can navigate
  * Auto-runs IQ pull-all once per customer (B-slice) when mode != off; cached in localStorage
  * Dispatcher applies generated use cases / archetype picks / exec briefs back into existing stores
