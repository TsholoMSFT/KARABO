feat(workspace): Phase 6.1 + 6.2 — backend status badge + estate templates

Phase 6 streamlining (first two wins):

src/components/BackendStatusBadge.tsx
  • Live dot in NavigationHeader (online/degraded/offline/checking)
  • Polls /api/health every 60s; click to force recheck
  • One-shot toast on online→offline and offline→online transitions
  • Tooltip shows last-checked timestamp
  • 8s fetch timeout via AbortController

src/components/NavigationHeader.tsx
  • Mounted BackendStatusBadge in the right slot of the full-variant
    header (replaces the empty spacer)

src/lib/solution-blueprint/estate-templates.ts
  • 5 one-click estate presets:
      – Standard Microsoft stack
      – Azure + AWS hybrid
      – Sovereign / regulated
      – Data + analytics modernisation
      – Greenfield / startup
  • applyEstateTemplate(base, id) — merges owned services (union),
    template wins for posture/identity/CICD/IaC fields

src/components/SolutionBlueprintWorkspace.tsx
  • New "Quick start: estate templates" card at top of the Estate tab.
    Each preset shows the description as a tooltip; one click and the
    estate fills with sensible defaults so architects don't have to
    pick through 30+ controls for every customer.
